# 国内镜像前缀：让 Dockerfile 基础镜像(maven/node/nginx/eclipse-temurin)与 redis
# 直接走国内源拉取，无需在 Docker Desktop 里配置 registry-mirrors。
#
# 用法：把下面的值改成任意“可前缀拉取 Docker Hub 官方镜像”的国内站，末尾带 /library/。
# 已验证可用的前缀（任选其一）：
#   docker.1ms.run/library/
#   docker.himirror.com/library/
#   hub.rat.dev/library/
#   docker.m.daocloud.io/library/
#
# 若想走官方 Docker Hub（需本机已配好 registry-mirrors），把值留空： REGISTRY=
REGISTRY=docker.1ms.run/library/
